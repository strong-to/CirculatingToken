# CDN 配置 - 阿里云 + 宝塔面板
# 当前使用服务器地址作为图片源

# CDN 基础地址（不要以 / 结尾）
# 使用服务器 IP 地址
NEXT_PUBLIC_CDN_BASE_URL=http://47.238.229.11

# 是否启用 CDN（true/false）
NEXT_PUBLIC_USE_CDN=true

# 注意：
# 1. 如果宝塔面板配置了域名，可以改为域名（如：http://www.example.com）
# 2. 如果后续配置了阿里云 CDN，修改上面的 NEXT_PUBLIC_CDN_BASE_URL 为 CDN 地址
# 3. 配置后需要重新构建：npm run build
