## 1. 表单持久化
- [ ] 1.1 审核所有步骤组件，确保读取 `launchpadData` 中的值并在用户输入时触发 `onDataChange`（包含第三步子组件及需要从 props 回填的本地状态）。
- [ ] 1.2 检查步骤导航（步骤条和按钮）使用的共享状态不会被重置，并在页面重新挂载时仍能恢复最新数据。

## 2. 第三步上传反馈
- [ ] 2.1 让文件上传模块支持既有的 `uploadedFileInfo` 与 `presetContent`，回到第三步时可直接回显。
- [ ] 2.2 上传成功后展示文件名、默认说明和成功提示，失败时给出可重试的错误消息。

## 3. 第七步预览
- [ ] 3.1 构建只读预览视图，渲染第一至第六步的文本、图片、文件和数值数据，不出现可编辑输入。
- [ ] 3.2 对缺失字段显示“未提供”等占位提示，并确保前序步骤更新后预览可立即同步。

## 4. 验收
- [ ] 4.1 人工验证：依次填写各步骤、来回切换与刷新，确认数据仍在；上传文件后看到成功提示；第七步预览与前面输入一致。
- [ ] 4.2 运行 `npm run lint`，确保无类型或 Lint 报错。
